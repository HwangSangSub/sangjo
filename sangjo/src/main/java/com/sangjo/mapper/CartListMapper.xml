<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sangjo.mapper.CartListMapper">

    <select id="selectCartList" resultType="com.sangjo.vo.CartListVO" parameterType="String">
        SELECT c.cart_no ,
               c.product_no ,
               c.member_id ,
               c.reg_date ,
               p.product_name ,
               p.product_price  ,
               p.product_img 
        FROM tbl_cart c
        JOIN tbl_product p 
        ON c.product_no = p.product_no
        WHERE c.member_id = #{MemberId}
    </select>

</mapper>