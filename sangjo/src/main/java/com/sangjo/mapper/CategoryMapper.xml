<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sangjo.mapper.CategoryMapper">

	<select id="selectList" resultType="com.sangjo.vo.CategoryVO">
		select category_no,
		category,
		category_detail,
		category_name,
		cd_name
		from tbl_category
		order by category_no
	</select>
</mapper>